(() => {
  function find_max(nums: number[]) {
    let max_num = Number.NEGATIVE_INFINITY;
    for (let num of nums) {
      if (num > max_num) {
        // // // // max_num wwww= num
      }

    }
    return max_num
  }

  console.log(find_max([50,35,65,7,8,96,1]))

})()
